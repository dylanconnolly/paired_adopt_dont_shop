Favorites

<% if @pets.count == 0 %>
  <p>You do not have any favorite pets.</p>
<% end %>
<% @pets.each do |pet| %>
  <section class="fav-pet" id="pet-<%= pet.id %>">
    <p><%= link_to "#{pet.name}", "/pets/#{pet.id}" %></p>
    <%= image_tag(pet.image) %>
    <%= link_to "Remove From Favorites", "/favorites/#{pet.id}", method: :delete %>
  </section>
<% end %>
